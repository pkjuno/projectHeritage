# 서버 실행 포트
PORT=3000

# 서버 실행 환경 (development | production | test)
NODE_ENV=development

# 데이터베이스 접속 정보 (예시 - 실제 사용하는 DB에 맞게 수정)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=project_heritage
DB_USER=root
DB_PASSWORD=changeme

# 프론트엔드(CORS 허용) 오리진
CLIENT_ORIGIN=http://localhost:8080
